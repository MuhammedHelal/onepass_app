import 'package:flutter/material.dart';

class WebsiteItem extends StatelessWidget {
  const WebsiteItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
